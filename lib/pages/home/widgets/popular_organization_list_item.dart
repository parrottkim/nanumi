import 'package:flutter/material.dart';
import 'package:nanumi/models/organization.dart';

class PopularOrganizationListItem extends StatelessWidget {
  const PopularOrganizationListItem({
    Key? key,
    required this.item,
  }) : super(key: key);

  final Organization item;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
